<%= turbo_frame_tag dom_id(post) do %>
  <div class="card card-body">
    <h2><%= post.name %> </h2>
    <p> <%= post.body %> </p>

    <div class="mt-2">
      <%= link_to "Edit", edit_post_path(post) %>
    </div>
  </div>
<% end %>